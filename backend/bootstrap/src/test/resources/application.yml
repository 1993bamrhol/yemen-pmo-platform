spring:
  datasource:
    url: jdbc:h2:mem:yemen_pmo_test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

security:
  jwt:
    secret: dGVzdC1vbmx5LWp3dC1rZXktZm9yLXllbWVuLXBtby1wbGF0Zm9ybS0yMDI2

bootstrap:
  admin:
    password: TestOnlyAdminPassword!2026

app:
  cors:
    allowed-origins: http://localhost:3000
