# Frontend

Next.js + TypeScript portal for the Yemen PMO site.

## Scripts
- `npm run dev`
- `npm run build`
- `npm run start`
- `npm run lint`

## Environment
Copy `.env.example` to `.env.local` and set the backend URL if needed.

